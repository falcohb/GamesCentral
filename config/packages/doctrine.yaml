doctrine:
    dbal:
        url: '%env(resolve:DATABASE_URL)%'

        # IMPORTANT: You MUST configure your server version,
        # either here or in the DATABASE_URL env var (see .env file)
        #server_version: '16'

        profiling_collect_backtrace: '%kernel.debug%'

    orm:
        # Tu n'as pas d'entités Doctrine dans ce projet.
        # On désactive l'auto-mapping et on supprime le mapping App vers src/Entity.
        auto_mapping: false

        # Optionnel : tu peux laisser ces options, mais elles n'ont pas d'effet sans mappings.
        # validate_xml_mapping: true
        # naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
        # identity_generation_preferences:
        #     Doctrine\DBAL\Platforms\PostgreSQLPlatform: identity

        mappings: { }

        # Cette section n'est pas nécessaire ici, surtout sans ORM/mappings.
        # controller_resolver:
        #     auto_mapping: false

when@test:
    doctrine:
        dbal:
            # "TEST_TOKEN" is typically set by ParaTest
            dbname_suffix: '_test%env(default::TEST_TOKEN)%'

# IMPORTANT :
# Ces caches ORM sont inutiles si tu n'utilises pas l'ORM. Tu peux les supprimer.
# Je les laisse commentés pour éviter tout effet de bord.
#when@prod:
#    doctrine:
#        orm:
#            query_cache_driver:
#                type: pool
#                pool: doctrine.system_cache_pool
#            result_cache_driver:
#                type: pool
#                pool: doctrine.result_cache_pool
#
#    framework:
#        cache:
#            pools:
#                doctrine.result_cache_pool:
#                    adapter: cache.app
#                doctrine.system_cache_pool:
#                    adapter: cache.system
